apiVersion: v1
kind: Service
metadata:
  name: phpfpm
  namespace: <%= namespace %>  
spec:
  selector:
    app: phpfpm
  type: NodePort
  ports:
  - port: 9000
    protocol: TCP
    targetPort: 9000
